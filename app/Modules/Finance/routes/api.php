<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Finance — API routes
|--------------------------------------------------------------------------
|
| Versioned JSON API routes, mounted at /api/v1 behind Sanctum.
| Middleware, URI prefix and route-name prefix come from the "api" entry
| in config/modules.php, so declare paths here without repeating them.
|
*/
